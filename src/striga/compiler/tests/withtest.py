from __future__ import with_statement

#Comment test

def formatweekday(self, day, width):
	with TimeEncoding(self.locale.name.bla) as encoding:
		if width >= 9:
			names = day_name
			ble = bla
			bla = bli
		else:
			names = day_abbr = 0
